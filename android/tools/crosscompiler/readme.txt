Tools are put into win and linux directories respectively.

makeandroid

This script will build an fpc cross compiler for linux-arm, to be used with Android. You'll also need another script called setfpcpath which sets the PATH for your fpc compiler.

makeandroidrelease

This script builds the relase version of the fpc crosscompiler. Only to be used when compiling a release.
